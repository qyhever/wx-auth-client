import Vue from 'vue'
import { Button, Cell, NavBar, Uploader, Dialog, Image, Row, Skeleton } from 'vant'

Vue
  .use(Button)
  .use(Cell)
  .use(NavBar)
  .use(Uploader)
  .use(Dialog)
  .use(Image)
  .use(Row)
  .use(Skeleton)

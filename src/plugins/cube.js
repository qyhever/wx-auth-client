import Vue from 'vue'
import {
  /* eslint-disable no-unused-vars */
  Style,
  Button,
  Loading,
  Scroll,
  CascadePicker
} from 'cube-ui'

Vue
  .use(Button)
  .use(Loading)
  .use(Scroll)
  .use(CascadePicker)
